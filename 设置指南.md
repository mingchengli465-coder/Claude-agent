# 设置指南

代码已经写好了。这份文档是你要做的部分，一步一步来，不用懂技术。

**一共四步，前两步是去拿钥匙，后两步是上线。**

做完一步就停下，不用一口气做完。

---

## 第 1 步：拿 Claude 的钥匙

1. 打开 https://console.anthropic.com/settings/keys
2. 注册/登录
3. 点 **Create Key**，起个名字随便填
4. **复制出来存好** —— 它只显示这一次，关掉就再也看不到了

还要充值，不然用不了：https://console.anthropic.com/settings/billing

充 5 美金就够跑很久了。

> 注意：这个跟你平时用的 Claude 订阅**不是一回事**，订阅的钱不能用在这里，得单独充。

---

## 第 2 步：拿推特的钥匙（这步最容易出错，仔细看）

1. 打开 https://developer.x.com/en/portal/dashboard
2. 用你的推特账号登录，建一个 Project，再建一个 App

### ⚠️ 关键的一步，漏了后面一定失败

3. 在 App 里找到 **User authentication settings**，点 **Set up**
4. App permissions 选 **Read and write**（要读也要写）
5. 保存

### ⚠️ 然后必须重新生成钥匙

6. 回到 **Keys and tokens** 页面
7. 把 **Access Token and Secret** 点 **Regenerate** 重新生成一次

**为什么：** 你改权限之前生成的钥匙，还是旧的只读权限。不重新生成，程序发不出推，会报错。这一步漏掉的人最多。

8. 最后你手上要有四个值，都存好：
   - API Key
   - API Secret
   - Access Token
   - Access Token Secret

---

## 第 3 步：先在自己电脑上试一下（可选，但建议）

如果你电脑上能跑 Python：

```bash
pip install -r requirements.txt
cp .env.example .env
```

把 `.env` 打开，把第 1、2 步拿到的五个值填进去。

然后先体检：

```bash
python x_bot.py doctor --write
```

它会逐项检查，哪里不对就告诉你哪里不对，还给你链接。全绿了再往下。

空跑看看它想发什么（不会真发）：

```bash
DRY_RUN=true python x_bot.py both
```

---

## 第 4 步：放到服务器上 24 小时跑

选 Render 或者 Railway 都行，一个月大概 5 美金。

**Render：**

1. 打开 https://render.com 注册，用 GitHub 登录
2. 选 **New** → **Blueprint**
3. 选这个仓库 —— 它会自动读 `render.yaml`，不用你配
4. 它会问你要那五个钥匙，填进去
5. 部署

**Railway：** 更简单，它会自动读 `Procfile`，同样把五个钥匙填到环境变量里就行。

> ⚠️ 变量要一个一个手敲，**不要**把 `.env.example` 整份粘进 Railway 的 Variables。
> 粘进去的话，`TELEGRAM_BOT_TOKEN` 会变成示范值 `your-telegram-bot-token`，
> 启动时就会报 `telegram.error.InvalidToken`。
> 现在程序遇到空值或示范值会直接停下来，并在日志里写清楚是哪个变量。

### 上线后先观察一天

`render.yaml` 里我把 `DRY_RUN` 默认设成了 `true`，意思是**只演练不真发**。

你先看一天日志，确认 AI 写的东西你满意，再把它改成 `false` 正式开跑。

---

## 关于存档

程序会记一些东西（发到哪个主题了、回过谁了）。这些存在服务器本地，重新部署会清掉。

**不用管它。** 清掉的后果很小：主题从头轮一遍，回复从最新的消息开始算，不会把老消息翻出来乱回。

---

## 花钱

| 项目 | 大概多少 |
|---|---|
| 服务器 | ~$5/月 |
| Claude | 看用量，一天几条推的话很少 |
| 推特接口 | 发推便宜，读消息按条算（这块是大头）|

嫌贵就调 `.env` 里的 `POLL_INTERVAL_SECONDS`（查得少就便宜）和 `MAX_THREAD_CONTEXT`（翻的上下文少就便宜）。

---

## 卡住了

把报错信息或者 `python x_bot.py doctor` 的输出发给我就行。
